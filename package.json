{
  "name": "design-mode",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "dev": "npm run dev -w demo",
    "build": "npm run build -w demo",
    "test": "node --test 'packages/claude-design-mode/test/*.test.mjs'"
  },
  "engines": {
    "node": ">=20"
  }
}
