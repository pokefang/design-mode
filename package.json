{
  "name": "design-mode",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "dev": "npm run dev -w demo",
    "build": "npm run build -w demo",
    "test": "npm test -w claude-design-mode"
  },
  "engines": {
    "node": ">=20"
  }
}
